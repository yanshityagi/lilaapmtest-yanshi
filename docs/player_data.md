# Player dataset location

Binary match data has been moved out of Git tracking to keep PR updates text-only.

## Expected local path

Place the dataset at:

- `./player_data/`

This folder is now ignored by Git and can be populated from shared storage, artifact storage, or Git LFS in a separate data repository.
